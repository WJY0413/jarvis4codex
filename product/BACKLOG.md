# v0.1.0 Backlog

1. Add mock adapter and contract-test suite.
2. Define JSON schemas and a SQLite migration boundary.
3. Implement `jarvisctl health`, `schedule list`, and `monitor list` against a non-production fixture store.
4. Port Codex App Server operations behind `CodexAppServerAdapter` with read-only preflight first.
5. Add Windows Service packaging and an upgrade/rollback runbook.
